# Same dependency set as pyproject.toml [project.dependencies] — change both when updating.
# Python >=3.11 (see pyproject.toml requires-python).
# gemstone_utils 0.5.0a2 (gu-0.5-test integration). PyPI when published; local: pip install -e ../gemstone_utils
# Install from metadata: pip install .
gemstone_utils==0.5.0a2
tzdata; platform_system == "Windows"
Flask
gunicorn
pydantic>=2.0
python-json-logger>=2.0.7
webexpythonsdk
SQLAlchemy
ldap3
psycopg[binary]>=3.2

# Optional: Sphinx HTML docs (see docs/sphinx_config/requirements.txt).
#   pip install -r docs/sphinx_config/requirements.txt
#   make -C docs/sphinx_config html
# Or: pip install ".[docs]"
